html(lang="pt-br")
  - let name = 'Curso de Front End'
  - const txt = true
  head
    meta(charset="utf-8")
    meta(viewport="width=device-width, initial-scale=1")
    title= name
  body
    p=name
    div.container-fluid
      if txt
        header #{name}
      else
        div nao existe txt
